services:
  - type: web
    name: test
    runtime: python
    plan: free
    buildCommand: ./build.sh
    startCommand: gunicorn config.wsgi:application
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: planificador-562x.onrender.com
      - key: CSRF_TRUSTED_ORIGINS
        value: https://planificador-562x.onrender.com
